# my_stickies 개발 계획

macOS 앱 Hold My Notes(https://holdmynotes.app/)의 Windows 클론. 기술 스택은 C# / WPF / .NET 9, 4K 고DPI 환경 기준.

## 완료

- [x] 우측 가장자리 도킹 투명 창 (작업 표시줄, Alt+Tab 미노출)
- [x] 휴면 알약 -> 호버 시 팬아웃 -> 탭 호버 시 확장 애니메이션
- [x] PerMonitorV2 DPI 인식
- [x] + 버튼 고정 오버레이, 덱 표시 개수 5장 제한 (최근 노트 우선)
- [x] 팬아웃 중 카드와 + 버튼 사이 이동 시 접힘 방지 (투명 호버 영역)
- [x] 우클릭 메뉴로 종료

## 진행 중 (이번 단계)

- [x] 확장된 스티커 클릭 시 편집 모드 (제목칸, 본문칸 개별 편집)
- [x] 스티커 우측 상단 삭제 버튼
- [x] 기본 제목 "새 메모 (현재시간)", 제목 미입력 시 자동 적용
- [x] SQLite 파일(%LocalAppData%\MyStickies\my_stickies.db)에 메모 저장

## TODO

### 핵심 기능
- [x] 트레이 아이콘 및 우클릭 메뉴 (새 메모, 종료) / 좌클릭으로 덱 펼치기
- [x] 트레이 메뉴에 메모 관리 열기 추가
- [x] 트레이 메뉴에 설정 추가 (메모 저장 위치)
- [x] 노트 색상 변경 (편집 모드 하단 색상 선택)
- [x] 숨김(Mark complete 대응) 처리 -> 덱에서 제거, 메모 관리 창에서 복원
- [x] 메모 관리 창 (전체 / 활성 / 숨김 필터, 검색, 미리보기)
- [x] 숨긴 메모 복원 및 영구 삭제 (메모 관리 창)
- [x] 편집 종료 단축키 정리 (Esc 취소, Ctrl+Enter / Ctrl+S 저장, 바깥 클릭 저장)
- [x] 덱에서 삭제 시 두 단계 확인 (× -> 3초간 "삭제" 버튼 -> 다시 클릭). 메모 관리 창은 확인 대화상자

### 덱 UX
- [x] 메모 순서 변경 (덱에서 카드 드래그, sort_order 컬럼 스키마 v3)
- [x] 확장 카드 높이를 본문 길이에 따라 가변 처리 (200 ~ 480 DIP, 초과 시 본문 스크롤)
- [x] 덱 최대 표시 개수, 시작 위치, 접힘 지연 시간 설정화 (설정 창)
- [x] 메모가 0장일 때 알약에 빈 점 하나 표시
- [x] 전역 단축키 Ctrl+Alt+S 덱 열기/닫기, Ctrl+Alt+N 새 메모 작성 (설정에서 끔 가능)

### 시스템 연동
- [x] 다른 앱이 같은 모니터에서 전체화면일 때 자동 숨김 (1초 주기 감시, 설정에서 끔 가능)
- [x] 다중 모니터 중 도킹 모니터 선택 (설정 창, 모니터별 DPI 반영)
- [x] 작업 영역 변경 대응: 해상도/모니터 구성, 작업 표시줄 위치·크기, 모니터 배율(DPI) 변경 시 재배치 (300ms 디바운스)
- [x] Windows 시작 시 자동 실행 옵션 (설정 창 체크박스, HKCU Run 키, 게시된 exe 기준)

### 데이터
- [x] 데이터 디렉토리(sqlite db 파일 위치) 지정 기능
  - [x] 최초 실행 시 저장 위치를 물어봄 (기본값 %LocalAppData%\MyStickies), 설정은 로컬 settings.json
  - [x] 트레이/우클릭 메뉴 설정에서 위치 변경, 재시작 없이 전환
  - [x] 새 폴더에 db 파일이 없으면 현재 메모 복사 여부를 물은 뒤 생성, 있으면 그대로 사용
  - [x] 외부(다른 PC 동기화)에서 파일이 바뀌면 감지해 자동 재로드 (편집 중이면 편집 후)
  - 단일 사용자 전제. 동시 편집 충돌은 고려하지 않음 (마지막 쓰기 우선)
- [x] 내보내기 (선택 메모: Markdown/평문, 전체: Markdown 한 파일)
- [x] 가져오기 (.md/.txt 여러 파일, "# 제목" 헤딩 또는 파일 이름을 제목으로)
- [x] DB 스키마 버전 관리 (PRAGMA user_version, v1 -> v2 자동 마이그레이션)

### 품질 및 배포
- [x] 글꼴 선택 (시스템 설치 글꼴 목록, 제목·본문 일괄) 및 본문 크기 조절 (10~24pt)
- [x] 단일 exe 게시 스크립트 publish.ps1 (Release, win-x64, 런타임 포함, %LocalAppData%\Programs\MyStickies)
- [x] 앱 아이콘 (Assets/app.ico, 스크립트로 생성)
- [x] README.md 작성 (소개, 기능, 빌드/게시 방법, 데이터 위치와 동기화 폴더 사용법, 단축키, 설정, 구조)

## 제외 (2026-09-17 결정)

- 설치 프로그램/MSIX 패키징, UI 자동 검증 스크립트, 다크 모드는 당분간 진행하지 않음
- 앱 자체 동기화 기능(클라우드 계정 등)은 제외. 동기화 폴더에 DB를 두는 방식으로 대체
- 동기화 동시 편집 충돌 처리는 단일 사용자 전제로 제외
